import bogus from "bogus.ts";

try {

    const c = ``.test(new RegExp("\s", "u")) == false;

    const b:undefined = 5;

    const a:any = bogus(bs);

    let v = "result: ${ a.result }";

    throw undefined;

} catch (e) {

    throw e;

}